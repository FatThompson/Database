cmd.exe /k "mysql -u root -padmin"
::Launch DMC from the folder, this will auto CD, 
::then wil excute the mysql by user root, with password admin
::It is bad practice to show the password in text,
::but this is a dev database